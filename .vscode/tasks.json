{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/DataCSharp.csproj"
            ],
            "problemMatcher": "$msCompile"
        },
        {
            "label": "run tests",
            "type": "shell",
            "command": "dotnet test"
        }
    ]
}